# Git шпаргалка для командной разработки
*(полный гайд для начинающих: develop, feature и main)*

Этот документ объясняет:
- как работать с Git безопасно,
- когда использовать `main`,
- как создавать ветки и делать merge,
- как получать аккуратное дерево коммитов.

---

## 1. Основные ветки проекта

| Ветка | Когда используется |
|------|-------------------|
| `main` | Только стабильная версия (релиз) |
| `develop` | Основная ветка разработки |
| `<task-branch>` | Любая новая задача |

> ❗ **Никогда не писать код напрямую в `main` и `develop`**

---

## 2. Когда и зачем нужна `main`

### `main` — это:
- версия проекта, которую можно показывать,
- финальный результат,
- то, что считается «готовым».

### В `main`:
- ❌ не пишут новый код
- ❌ не делают быстрые правки
- ✅ попадают **только проверенные изменения из `develop`**


## 3. Работа начинается ВСЕГДА с `develop`

### Команды
```bash
git checkout develop
git pull
```


## 4. Создание ветки для задачи

### Шаблон имени ветки

```<task-branch>```
### Примеры

```
feature-login
add-user-page
fix-order-bug
```

### Команда

```bash
git checkout -b <task-branch>
```

**Что делает:**
Создаёт новую ветку для задачи и сразу переключает на неё.

---

## 5. Работа с кодом → коммит

### Проверить изменения

```bash
git status
```

### Добавить изменения

```bash
git add .
```

### Создать коммит

```bash
git commit -m "описание изменений"
```

---

## 6. Отправка ветки на GitHub

### Первый раз

```bash
git push -u origin <task-branch>
```

### Далее

```bash
git push
```

---

## 7. Слияние ветки в `develop`

### Переключиться

```bash
git checkout develop
git pull
```

### Слияние (важно)

```bash
git merge --no-ff <task-branch>
```

### Отправить результат

```bash
git push
```

### Почему `--no-ff`

Используется, чтобы в истории Git было видно, что работа велась в отдельной ветке.

---

## 8. Удаление ветки после merge

```bash
git branch -d <task-branch>
git push origin --delete <task-branch>
```

**Зачем:**
Чтобы не засорять проект старыми ветками.

---

## 9. Когда переходить в `main`

Переход в `main` нужен, когда:

* разработка завершена;
* ветка `develop` стабильна;
* нужно зафиксировать релиз.

---

## 10. Слияние `develop → main` (РЕЛИЗ)

### Переключиться на `main`

```bash
git checkout main
git pull
```

### Слияние

```bash
git merge --no-ff develop
```

### Отправить на GitHub

```bash
git push
```

После этого версия в `main` считается **готовой и стабильной**.

---

## 11. Проверка истории (красивое дерево)

```bash
git log --oneline --graph --decorate --all
```

### Правильная структура

```
*   Merge branch 'develop' into main
|\
| *   Merge branch '<task-branch>' into develop
| |\
| | * commit
| |/
|/
* main
```

---

## 12. Частые ошибки

### ❌ Закоммитил в `main`

```bash
git reset --soft HEAD~1
git checkout develop
git commit -m "commit message"
```

---

### ❌ Забыл обновить ветку

```bash
git checkout develop
git pull
```

---

## 13. Короткая шпаргалка (всё в одном месте)

```bash
# старт
git checkout develop
git pull

# задача
git checkout -b <task-branch>
git add .
git commit -m "message"
git push

# merge в develop
git checkout develop
git merge --no-ff <task-branch>
git push

# очистка
git branch -d <task-branch>
git push origin --delete <task-branch>

# релиз
git checkout main
git merge --no-ff develop
git push
```

---

## 14. Главное правило

* **`main`** — только релиз
* **`develop`** — работа команды
* **`<task-branch>`** — одна задача





# Зачем удалять ветку после merge

Когда ты делаешь merge ветки `<task-branch>` в `develop`:

- все изменения **уже сохранены** в `develop`;
- коммиты **никуда не пропадают**;
- ветка **выполнила свою задачу**.

👉 После этого ветка становится **ненужной**.

---

## Что реально происходит при удалении ветки

### ВАЖНО
> Удаляется **только указатель на ветку**,  
> код и коммиты **НЕ удаляются**, если они уже смержены.

---

## Пример

### До merge
```

A --- B --- C   (develop)

D --- E   (<task-branch>)

```

### После merge
```

A --- B --- C --- D --- E   (develop)

```

### После удаления ветки
```

A --- B --- C --- D --- E   (develop)

```

**Ничего не пропало.**

---

## Что будет, если УДАЛИТЬ ветку

### Плюсы
- список веток чистый;
- Git Graph аккуратный;
- не путаешься, где старая задача, а где новая;
- меньше риска случайно продолжить работу в старой ветке.

### Минусы
- ❌ нет (если merge уже был).

---

## Что будет, если НЕ удалять ветку

Ничего не сломается ❗  
Но со временем появятся проблемы.

### Минусы
- десятки старых веток:
```

feature-login
feature-login-final
feature-login-fix
test123

````
- сложно понять:
- какие ветки активные;
- какие уже смержены;
- легко случайно продолжить работу в старой ветке;
- Git Graph превращается в «кашу».

---

## Когда НЕЛЬЗЯ удалять ветку

❌ **НЕ удаляй ветку, если:**
- merge ещё не сделан;
- есть незакоммиченные изменения;
- ветка нужна для доработки;
- идёт code review.

---

## Безопасное правило

> **Если ветка смержена — её можно удалять**  
> **Если не смержена — Git не даст удалить**

### Команда
```bash
git branch -d <branch-name>
````

Если ветка **не смержена**, Git остановит тебя:

```
error: The branch '<branch-name>' is not fully merged.
```

---

## Разница между `-d` и `-D`

| Команда         | Что делает              | Когда использовать |
| --------------- | ----------------------- | ------------------ |
| `git branch -d` | Безопасное удаление     | После merge        |
| `git branch -D` | Принудительное удаление | Только если уверен |

### ❗ ВАЖНО

`-D` **может привести к потере работы**, если ветка не была смержена.

---

## Удаление ветки на GitHub

```bash
git push origin --delete <branch-name>
```

Что происходит:

* удаляется **только удалённая ветка**;
* локальные коммиты остаются;
* при необходимости ветку можно восстановить.

---

## Как восстановить удалённую ветку

### Если ветка была смержена

```bash
git checkout -b <branch-name> develop
```

---

### Если ветка была удалена случайно (НЕ смержена)

1. Найти коммит:

```bash
git reflog
```

2. Восстановить ветку:

```bash
git checkout -b <branch-name> <commit-hash>
```

---

## Реальный кейс с ошибкой

### Ситуация

Ты случайно сделал:

```bash
git branch -D feature-login
```

### Что делать

1. Посмотреть историю:

```bash
git reflog
```

2. Найти последний коммит ветки:

```
abc1234 HEAD@{3}: commit: add login form
```

3. Восстановить:

```bash
git checkout -b feature-login abc1234
```

✔ Ветка восстановлена.

---

## Коротко (если объяснять другу)

> Ветка — это временная рабочая зона.
> Закончил задачу → слил → убрал за собой.

---

## Итог

| Действие                  | Результат              |
| ------------------------- | ---------------------- |
| Удалить ветку после merge | Чистота и порядок      |
| Не удалять                | Беспорядок со временем |
| Удалить до merge          | Потеря работы ❌        |


